// Sử dụng 'const' cho các giá trị cấu hình không thay đổi
const testCaseName: string = "Verify Login Functionality";

// Sử dụng 'number' cho các trị số đếm
const retryCount: number = 3;

// Sử dụng 'boolean' cho các công tắc (on/off)
const isCaptureScreenshot: boolean = true;

console.log("Tên bài test: ", testCaseName);